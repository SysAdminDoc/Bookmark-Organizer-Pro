# v6.6.13 OPDS Serving Audit

Date: 2026-06-06

## Findings

- R-26 needed loopback serving after the v6.6.12 file-export slice.
- The existing local API already binds to `127.0.0.1`, which is the safest
  initial serving surface for a bookmark catalog.
- OPDS file export and HTTP serving should share a renderer to prevent drift.

## Changes Shipped

- Added `render_opds()` for shared XML rendering.
- Added local API `GET /opds` route.
- Added tag/category/title/limit query support.
- Added OPDS content type response headers.
- Added API route test coverage.
- Bumped release metadata to v6.6.13.

## Verification

- `py -3.12 -m compileall -q bookmark_organizer_pro tests packaging` passed.
- `py -3.12 -m pytest -q tests/test_core.py::TestMainAppManagers::test_bookmark_api_serves_opds_catalog tests/test_services.py::TestOPDSExport tests/test_cli.py::TestCLIExportSubcommands::test_opds_export tests/test_packaging.py tests/test_browser_extension.py` passed with 16 tests.
- `py -3.12 -m pytest -q` passed with 322 tests.

## Remaining Follow-Up

- Consider category/tag navigation feeds if e-reader clients need browse
  hierarchy beyond a filtered acquisition feed.
