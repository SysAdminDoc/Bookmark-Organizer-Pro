# v6.6.29 MCP Stdio Contract Audit

## Scope

R-15 advanced from in-process FastMCP contract validation to subprocess stdio
transport validation. The new smoke verifies that clients can discover the chat
stream contract metadata through an actual MCP stdio session.

## Changes

- Added a subprocess stdio MCP client smoke.
- The smoke starts `python -m bookmark_organizer_pro.mcp_server` with an
  isolated data directory.
- The smoke initializes `mcp.client.session.ClientSession`.
- The smoke calls `list_tools()` and verifies `chat_with_collection_stream`
  stream contract metadata.

## Compatibility

- No public tool arguments changed.
- No chat model call is made.
- The smoke only validates tool discovery metadata.

## Verification

- `py -3.12 -m compileall -q bookmark_organizer_pro tests packaging`
- `py -3.12 -m pytest -q tests/test_mcp_tools.py::TestChatStreaming tests/test_mcp_tools.py::TestMCPRuntimeCompatibility` passed with 14 tests.
- `py -3.12 -m pytest -q tests/test_mcp_tools.py::TestChatStreaming tests/test_mcp_tools.py::TestMCPRuntimeCompatibility tests/test_packaging.py tests/test_browser_extension.py` passed with 28 tests.
- `py -3.12 -m pytest -q` passed with 360 tests.

## Remaining R-15 Work

- Validate stream contract and progress behavior over Streamable HTTP.
- Decide whether Anthropic and Google native streaming adapters should be added
  after SDK surface verification.
