# Install input is derived from pyproject.toml, the authoritative dependency
# definition. pylock.toml is the reproducible release resolution generated
# by scripts/package_contract_audit.py --update-lock.
.[ai,encryption,mcp,sunvalley,themedetect]
